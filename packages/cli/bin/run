#!/usr/bin/env node
console.log('HELLO THERE');
require('@oclif/command')
  .run()
  .then(require('@oclif/command/flush'))
  .catch(require('@oclif/errors/handle'))
  .finally(() => 'aight we outta here');
